const MaxSize = 1024;
export default class Timeline {
    x = new Array(MaxSize);
    y = new Array(MaxSize);
    vx = new Array(MaxSize);
    vy = new Array(MaxSize);
    duration = 5.0;
    add(x, y, vx, vy, time) {
    }
    update() {
    }
}
